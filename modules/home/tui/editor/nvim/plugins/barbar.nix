{ # The neovim tabline plugin. -- https://github.com/romgrk/barbar.nvim
  programs.nixvim.plugins.barbar = {
    enable = true;

    # keymaps = {
      # silent = true;

      # next = "<TAB>";
      # previous = "<S-TAB>";
      # close = "<C-w>";
    # };
  };
}
